namespace TPI_Integrador_Prog3.Enum
{
    public enum UserType //Declaramos un Enum, es decir un tipo personalizado, al que le podemos asignar los valores que querramos.
    {
        Admin,
        Client
    }
}